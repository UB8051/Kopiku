<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kopiku">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/opening_bg"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailCatatanTransaksi"></activity>
        <activity android:name=".ListCatatanTransaksiActivity" />
        <activity android:name=".ListTransaksi" />
        <activity android:name=".LabaActivity" />
        <activity android:name=".PembayaranActivity" />
        <activity android:name=".TransaksiActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".OpeningActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListProdukActivity" />
        <activity android:name=".DetailProdukActivity" />
        <activity android:name=".AddProdukActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>